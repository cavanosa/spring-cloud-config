server:
  port: 8080
  
zuul:
  ignoredservices: "*"
  host:
    connect-timeout-millis: 20000
    socket-timeout-millis: 20000
    routes:
      sample-service:
        path: /echo/**
        url: http://localhost:8091
